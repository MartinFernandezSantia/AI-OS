# Santia Propiedades — Session Handoff (2026-06-14)

## ⚠️ Canonical repo (read first)
There are TWO clones on disk. Work ONLY in:
- **`/home/martin/AI-OS/projects/SantiaPropiedades`** ← canonical. Dev server (Firefox localhost:3000) runs from here.
- `/home/martin/dev-workspace/projects/SantiaPropiedades` ← STRAY copy. Has old uncommitted edits. Should be reset clean (`git checkout . && rm -rf app/vender-mi-propiedad app/cuanto-vale-mi-propiedad components/ui/FAQAccordion.tsx`) to avoid split-brain.

## Branch
`seo/geo-report-fixes` (off `main` @ ab49bdd) — **local only, not pushed, no PR yet.** 3 commits:
1. `0125933` feat(seo): SEO/GEO report fixes
2. `eb44063` fix(seo): remove free-tasacion claims
3. `533c83c` feat(media): real office photos replace stock

## Done (from santiapropiedades-seo-geo-report-2026-06-13.md)
- Property detail `generateMetadata` (title/desc/canonical/OG/Twitter) + Product+Offer JSON-LD
- Per-page OpenGraph on all pages; removed meta keywords
- Phone NAP fixed `tel:+542236846129`
- New `/vender-mi-propiedad` + `/cuanto-vale-mi-propiedad` (FAQ + FAQPage/Service JSON-LD), in sitemap, linked from footer/home
- CSR "Cargando propiedades" fallback resolved
- **Real photos**: 5 optimized webp in `public/office/` (storefront x2, Daniel-at-desk, reception, portrait) w/ -640/-1280 hero variants. `lib/images.ts` pool swapped stock→real. Explicit placement + accurate alt across all 7 content pages. `/house4.jpg` backgrounds swapped.

## Pending / next
- **Push branch + open PR** (not done — was told to hold)
- **Production `pnpm build`** NOT run to completion (earlier attempt OOM-killed, exit 137). Verify before merge. Use `CI=true pnpm ...` (no-TTY pnpm aborts otherwise).
- **Staff-photo consent**: `oficina-atencion.webp` + `martillero-retrato.webp` show an identifiable employee + Daniel — held back pending Martin's OK to publish.
- **Photo repetition**: Daniel's desk shot now on ~6 pages. Higgsfield b-roll should add variety.
- Report items NOT done (deferred/structural): aggregateRating (needs real review count), barrio landing pages (ES2), martillero authority page + Person schema (ES5), blog (ES4), `/alquilar-mi-propiedad` (ES3).

## Higgsfield plan (next session — generate image sequence FIRST, then video)
- Home hero video: MdP coastal/city timelapse, golden hour, no faces (seed img → sequence → video)
- Barrio establishing images for future landing pages (Nueva Pompeya/Centro/Güemes)
- Interior b-roll stills (keys, documents, floor plan) to break desk-photo repetition

## Source assets
Raw office photos (40): `/home/martin/windows-shared/SantiaProp-new-office/` (5184×3888 JPEGs)
ffmpeg available (no ImageMagick). Loops in inline Bash get denied — use a written .sh script.
