{
  "nodes": [
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "version": 2
          },
          "combinator": "and",
          "conditions": [
            {
              "leftValue": "={{ ($json.seleccion && $json.seleccion.necesitaAclaracion === true) && (($json.seleccion.terminos || []).length === 0) && (($json.seleccion.productos || []).length === 0) }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ]
        },
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        600,
        0
      ],
      "name": "¿Necesita Aclaración?"
    }
  ],
  "connections": {}
}
