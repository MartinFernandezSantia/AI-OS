# Copiar a .env.local y completar. (Este archivo se llama .txt porque Claude tiene prohibido
# escribir archivos .env* — ver el deny rule en CLAUDE.md.)
#
# Sin estas dos variables el visor funciona igual para MIRAR los chunks; solo hace falta
# configurarlas para usar el botón de ingesta.

# Connection string de Postgres del rol bot_curator (least-privilege: select/insert/delete
# sobre bot.rag_catalog). Usar el SESSION POOLER de Supabase, puerto 5432 — el directo
# db.<ref>.supabase.co es IPv6-only. Usuario con la forma bot_curator.<project-ref>
BOT_DB=

# API key de Google AI Studio (Gemini) para generar los embeddings.
# OJO: tiene que ser el MISMO modelo que usa el nodo de embeddings en n8n
# (gemini-embedding-001) o los vectores no son comparables y el retrieval falla.
GEMINI_API_KEY=
